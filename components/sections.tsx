import Image from "next/image";
import { ArrowRight, Check, Compass, Mail, PackageCheck, Route, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { ProductCard } from "@/components/product-card";
import { products, services, site } from "@/lib/site";

export function Hero() { return <section className="bg-ink text-white"><div className="container-page grid min-h-[calc(100vh-64px)] items-center gap-10 py-12 lg:grid-cols-[0.88fr_1.12fr] lg:py-16"><div><p className="eyebrow text-brass">Automotive Components & Trade</p><h1 className="mt-5 max-w-3xl text-5xl font-bold leading-[1.02] sm:text-6xl lg:text-7xl">Parts programs begin with better visibility.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-white/70">TAIGAUTO CO., LTD. presents a focused starting point for automotive component sourcing, supplier coordination, and cross-border trade conversations.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Button href="/showcase">Explore Component Focus <ArrowRight size={18} /></Button><Button href="/contact" variant="secondary">Start an Inquiry</Button></div><div className="mt-9 grid grid-cols-3 gap-4 border-t border-white/15 pt-6 text-sm">{["Component focused", "Specification ready", "Trade minded"].map((item) => <div key={item} className="flex items-start gap-2 font-semibold text-white/70"><Check size={16} className="mt-0.5 shrink-0 text-brass" />{item}</div>)}</div></div><div className="relative"><div className="absolute -left-4 top-8 z-10 hidden border border-white/10 bg-ink p-4 shadow-soft md:block"><p className="text-xs font-bold uppercase tracking-widest text-brass">TAIGAUTO</p><p className="mt-1 text-sm font-bold">Ontario, Canada</p></div><div className="overflow-hidden border border-white/15 shadow-soft"><Image src="/images/automotive-supply-hero.png" alt="Automotive component supply workspace" width={1536} height={1024} className="h-auto w-full" priority /></div></div></div></section>; }
export function ProductGrid({ limit }: { limit?: number }) { const items = limit ? products.slice(0, limit) : products; return <section className="bg-pearl py-20"><div className="container-page"><p className="eyebrow text-wine">Component Focus</p><h2 className="mt-3 max-w-2xl text-4xl font-bold text-ink">Automotive categories made clear for B2B conversations.</h2><p className="mt-4 max-w-2xl leading-7 text-ink/65">The incorporation documents establish TAIGAUTO but do not specify product categories. These modular showcase areas express the automotive direction implied by the company name and can be replaced with approved catalogs or programs.</p><div className="mt-10 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{items.map((item) => <ProductCard key={item.name} product={item} />)}</div></div></section>; }
export function TrustBand() { const items = [{ icon: Compass, title: "Automotive oriented", text: "A professional frame for automotive component sourcing and supply discussions." }, { icon: Route, title: "Brief to program", text: "Conversations can move from component need to specifications, packaging, and shipment planning." }, { icon: ShieldCheck, title: "Built for discovery", text: "Search-ready foundations provide a clear, credible first view for prospective partners." }]; return <section className="bg-mist py-14"><div className="container-page grid gap-5 md:grid-cols-3">{items.map((item) => <div key={item.title} className="border border-ink/10 bg-white p-6"><item.icon className="text-wine" size={24} /><h3 className="mt-4 text-lg font-bold">{item.title}</h3><p className="mt-2 text-sm leading-6 text-ink/65">{item.text}</p></div>)}</div></section>; }
export function ServicesSection() { return <section className="bg-white py-20"><div className="container-page grid gap-10 md:grid-cols-[0.85fr_1.15fr] md:items-center"><div><p className="eyebrow text-wine">How We Work</p><h2 className="mt-3 text-4xl font-bold text-ink">Designed for a disciplined parts conversation.</h2><p className="mt-5 text-base leading-7 text-ink/70">TAIGAUTO gives buyers, distributors, suppliers, and trade partners a focused place to initiate automotive component discussions. Final product programs can be added as commercial details are confirmed.</p><Button href="/services" className="mt-7">Explore Capabilities</Button></div><div className="grid gap-3 sm:grid-cols-2">{services.map((service) => <div key={service} className="flex gap-3 border-l-2 border-brass bg-pearl p-4 text-sm font-semibold text-ink/75"><Check size={18} className="mt-0.5 shrink-0 text-wine" />{service}</div>)}</div></div></section>; }
export function ContactSection() { return <section className="bg-ink py-20 text-white"><div className="container-page grid gap-8 md:grid-cols-[0.85fr_1.15fr]"><div><p className="eyebrow text-brass">Contact TAIGAUTO</p><h2 className="mt-3 text-4xl font-bold">Start with the component brief.</h2><p className="mt-4 leading-7 text-white/70">Share the component category, intended market, sourcing need, or supplier partnership you would like to explore.</p><div className="mt-7 grid gap-3 text-sm font-semibold text-white/75"><a href={`mailto:${site.email}`} className="flex items-center gap-3"><Mail size={18} className="text-brass" />{site.email}</a><p className="flex items-center gap-3"><PackageCheck size={18} className="text-brass" />Automotive Components & Trade</p></div></div><form className="border border-white/15 bg-white p-5 text-ink shadow-soft"><div className="grid gap-4 sm:grid-cols-2"><label className="grid gap-2 text-sm font-bold">Name<input className="focus-ring min-h-12 border border-ink/10 px-3" placeholder="Your name" /></label><label className="grid gap-2 text-sm font-bold">Business email<input className="focus-ring min-h-12 border border-ink/10 px-3" placeholder="you@company.com" /></label></div><label className="mt-4 grid gap-2 text-sm font-bold">Component interest<input className="focus-ring min-h-12 border border-ink/10 px-3" placeholder="Braking, lighting, electrical, chassis..." /></label><label className="mt-4 grid gap-2 text-sm font-bold">Message<textarea className="focus-ring min-h-32 border border-ink/10 p-3" placeholder="Share the component requirement or trade opportunity." /></label><Button href={`mailto:${site.email}?subject=Automotive%20Component%20Inquiry`} className="mt-5 w-full">Send Component Inquiry</Button></form></div></section>; }
